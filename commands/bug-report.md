---
description: Generate a filled Bug Report completion writeup (root cause, fix, acceptance criteria, commits, PR) for a bug fixed this session
argument-hint: "[optional context about the bug]"
---

Invoke the `bug-report` skill to generate the team's Bug Report completion writeup for the bug fixed in this session. Auto-fill it from the actual work — read git for commits, check for an existing PR and create one if missing — and output a single paste-ready block.

Additional context from the user: $ARGUMENTS
